@model TestSystem.Models.TestShortModel[]
@{
    ViewBag.Title = "Home Page";
}

@foreach (var test in Model)
{
    @Html.Partial(MVC.Home.Views._ShortTest, test)
}

<div class="row">
    <div class="col-md-3"><div class="btn btn-success btn-block">Создать</div></div>
</div>